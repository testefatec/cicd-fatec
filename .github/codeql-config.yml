# Configuração do CodeQL para Python (apenas workflow customizada)
# Expande o conjunto de queries para aumentar a cobertura de alertas.

name: codeql-config
queries:
  - uses: security-extended
  - uses: security-and-quality
